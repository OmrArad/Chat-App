<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.AndroidTutorial" parent="Theme.AppCompat.Light.NoActionBar">
        <item name="android:windowNoTitle">true</item>
        <item name="android:windowActionBar">false</item>
    </style>


    <!-- Light Theme -->
    <style name="AppTheme.Light" parent="Theme.AppCompat.Light">
        <!-- Customize theme attributes -->
    </style>

    <!-- Dark Theme -->
    <style name="AppTheme.Dark" parent="Theme.AppCompat">
        <!-- Customize your dark theme attributes -->
        <item name="android:windowBackground">#BA454545</item>
        <item name="android:textColorPrimary">@color/white</item>
        <!-- Add more dark theme attributes as needed -->
    </style>

</resources>
